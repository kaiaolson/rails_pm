<h1>All Projects</h1>

<% @projects.each do |proj| %>
  <h2><%= proj.title %></h2>
  <p><%= proj.description %></p>
  <p><%= proj.due_date %></p>
  <p><%= link_to "View #{proj.title}", project_path(proj) %></p>
<% end %>
